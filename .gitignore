node_modules/
dist/
.env
*.log
.drizzle/
tsconfig.tsbuildinfo
almacen/

# app de escritorio
escritorio/node_modules
escritorio/salida

.env.produccion

# respaldo del .env de produccion, generado al separar entornos
.env.respaldo-produccion

# respaldos de la base: pueden tener datos de clientes
respaldos/

# app de android
movil/node_modules
movil/salida
movil/android/build
movil/android/app/build
movil/android/.gradle
movil/android/local.properties

# La firma de la app y su clave. Si esto se pierde, un telefono que ya tenga
# WhatsWV instalado no acepta la actualizacion: hay que desinstalar primero.
# Va aparte del repositorio y se respalda a mano.
movil/firma/

# el zip de la extension, que se arma con npm run exportador:empaquetar
herramientas/salida/
